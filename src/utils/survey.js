// src/utils/survey.js
import api from "./api";

/** 온보딩 설문 저장 */
export const saveSurvey = async (payload) => {
  // return api.post('/onboarding/survey', payload).then(r => r.data);
  throw new Error("TODO: 백엔드 URL 나오면 연결 (온보딩 설문 저장)");
};
